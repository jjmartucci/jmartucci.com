---
import {CollectionEntry} from "astro:content";
import PlantTag from "../PlantTag/PlantTag.astro";
import SlottedPreview from "./SlottedPreview.astro";
interface Props {
    note: CollectionEntry<"garden">
}
const {note} = Astro.props
console.log(note)
---

<SlottedPreview slug={note.slug} type="note" title={note.data.title} description={note.data.description} tags={note.data.tags}>
    <img src={note.data.image} slot="image"/>
</SlottedPreview>

<style>
    img {
        border-radius: 0;
    }
</style>

---
import {CollectionEntry} from "astro:content";
import PlantTag from "../PlantTag/PlantTag.astro";
import SlottedPreview from "./SlottedPreview.astro";
import {convertISOToMMMDDYYYY} from "../../utils/time";
interface Props {
    note: CollectionEntry<"garden">
}
const {note} = Astro.props
const { remarkPluginFrontmatter } = await note.render();

---

<SlottedPreview
        created={convertISOToMMMDDYYYY(remarkPluginFrontmatter.createdDate)}
        modified={convertISOToMMMDDYYYY(remarkPluginFrontmatter.modifiedDate)}
        slug={note.slug} image={note.data.image} type="note" title={note.data.title} description={note.data.description} tags={note.data.tags}>
</SlottedPreview>

<style>
    img {
        border-radius: 0;
    }
</style>
